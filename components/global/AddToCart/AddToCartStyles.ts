import {SxProps} from '@mui/material/styles'

export const AddToCart: SxProps = {
    display: 'flex',
    alignItems: 'center',
    position: 'relative'
}

export const Icon: SxProps = {
    color: 'primary.main'
}